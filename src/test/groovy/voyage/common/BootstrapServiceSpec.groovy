package voyage.common

import spock.lang.Specification

class BootstrapServiceSpec extends Specification {

    def 'super user default password should not update in test environment'() {
        //TODO: need to implement this test
    }

    def 'super user default password should update in other than test environment'() {
        //TODO: need to implement this test
    }

    def 'super user password should not update if password is not default password'() {
        //TODO: need to implement this test
    }
}
